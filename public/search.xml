<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>niteCTF 2025 - Lament of the Seraph</title>
      <link href="/2025/12/15/lament_of_the_seraph/"/>
      <url>/2025/12/15/lament_of_the_seraph/</url>
      
        <content type="html"><![CDATA[<h1 id="Lament-of-the-Seraph-Solution"><a href="#Lament-of-the-Seraph-Solution" class="headerlink" title="Lament of the Seraph Solution"></a>Lament of the Seraph Solution</h1><blockquote><p>Our friendly neighbourhood crypto bro is left lamenting after a ransomware attack wiped his fortune and locked his wallets. What he thought was a gateway to wealth was, in truth, a trap. Among his many missteps, some say he once placed his trust in a public server. Help him undo his folly and seize the flag in the process.</p><p><code>Note: This is a live malware, please exercise caution.</code></p><p>Drive Link: <a href="https://drive.proton.me/urls/ZD2V8HKZCM#fSqFaLtxBubo">https://drive.proton.me/urls/ZD2V8HKZCM#fSqFaLtxBubo</a></p><p>Password: infected</p></blockquote><p><strong>Flag</strong>: <code>nite{CRYPT0BR0Sn4NG3LS4tTH3g4t3}</code></p><p>We are presented with a 32-bit PE file - <code>seraphs_lance.exe</code></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">file seraphs_lance.exe<br>seraphs_lance.exe: PE32 executable (console) Intel 80386 (stripped to external PDB), for MS Windows, 9 sections<br></code></pre></td></tr></table></figure><p>First things first, let’s look at its decomp to get an idea of things. Going over the strings, we find a ransom note in <code>sub_402A22</code>:</p><p><img src="/images/posts/lament_of_the_seraph/1.png" alt=""></p><p>Okay, so we are presented with what looks like a ransomware designed to encrypt crypto wallets. This is a good starting point for us and we can consider this <code>main</code> for now, so let’s start looking at the functions being called in this function:</p><h3 id="sub-401D18"><a href="#sub-401D18" class="headerlink" title="sub_401D18"></a><code>sub_401D18</code></h3><p><img src="/images/posts/lament_of_the_seraph/2.png" alt=""></p><p>This is a pretty self-explanatory function. The function returns 1 if the binary is being debugged which in turn results in the binary being exited.</p><h3 id="sub-401E0E"><a href="#sub-401E0E" class="headerlink" title="sub_401E0E"></a><code>sub_401E0E</code></h3><p>Now this function calls a couple of hashed Windows API functions.</p><p><img src="/images/posts/lament_of_the_seraph/3.png" alt=""></p><p><code>sub_4019B1</code> parses the PE Header of the loaded DLL, walks the Export Directory, hashes every function name it finds using <code>sub_4014E0</code>, and compares it to the requested hash.</p><p><img src="/images/posts/lament_of_the_seraph/4.png" alt=""></p><p>So now we need to look at the hashing algorithm.</p><h3 id="sub-4014E0"><a href="#sub-4014E0" class="headerlink" title="sub_4014E0"></a><code>sub_4014E0</code></h3><p>The hashing algorithm is a modified version of <code>Salsa20</code>. The initial state is a 4x4 matrix that’s filled with the length of the string passed to the function. It then XORs the string with the string length. Then it uses custom “Nothing-Up-My-Sleeve” constants - “SRPH”, “LNCE”, “NITE” and “2025” instead of the standard “expa”, “nd 3”, “2-by”, and “te k”.</p><p>For example, the initial state for <code>VirtualAlloc</code> will look something like this:</p><div class="table-container"><table><thead><tr><th style="text-align:center"></th><th style="text-align:center"><strong>Col 0</strong></th><th style="text-align:center"><strong>Col 1</strong></th><th style="text-align:center"><strong>Col 2</strong></th><th style="text-align:center"><strong>Col 3</strong></th></tr></thead><tbody><tr><td style="text-align:center"><strong>Row 0</strong></td><td style="text-align:center"><code>Len</code> ^ “Virt” ^ “SRPH”</td><td style="text-align:center"><code>Len</code> ^ “ualA”</td><td style="text-align:center"><code>Len</code> ^ “lloc”</td><td style="text-align:center"><code>Len</code></td></tr><tr><td style="text-align:center"><strong>Row 1</strong></td><td style="text-align:center"><code>Len</code></td><td style="text-align:center"><code>Len</code> ^ “LNCE”</td><td style="text-align:center"><code>Len</code></td><td style="text-align:center"><code>Len</code></td></tr><tr><td style="text-align:center"><strong>Row 2</strong></td><td style="text-align:center"><code>Len</code></td><td style="text-align:center"><code>Len</code></td><td style="text-align:center"><code>Len</code> ^ “NITE”</td><td style="text-align:center"><code>Len</code></td></tr><tr><td style="text-align:center"><strong>Row 3</strong></td><td style="text-align:center"><code>Len</code></td><td style="text-align:center"><code>Len</code></td><td style="text-align:center"><code>Len</code></td><td style="text-align:center"><code>Len</code> ^ “2025”</td></tr></tbody></table></div><p>The algorithm then runs 4 Double-Rounds. Each Double-Round consists of a Column Round followed by a Row Round. Every transformation mixes four words using ARX. Usually, Salsa20 rotates by 7, 9, 13, 18. This variant rotates by 5, 11, 17, 23.</p><p>The algorithm adds the mixed 16-word state back to the original one to break linearity (feed-forward). Then it collapses the 512-bit state by XORing all 16 words together. The final 32-bit XOR result is returned as the hash.</p><p>Now a <a href="resolver.py">python script</a> can be written to resolve the functions with the hashes provided.</p><h3 id="sub-401E0E-continued"><a href="#sub-401E0E-continued" class="headerlink" title="sub_401E0E (continued)"></a><code>sub_401E0E</code> (continued)</h3><p>After resolving the hashes, the function looks something like this:</p><p><img src="/images/posts/lament_of_the_seraph/5.png" alt=""></p><p>It is now pretty easy to identify what this function is doing - it calls the Windows API function <code>IsWow64Process</code> via <code>kernel32.dll</code>, this function simply checks if the given process (via <code>GetCurrentProcess</code>) is running under <code>Windows-on-Windows-64-bit</code>.</p><h3 id="What-is-WOW64"><a href="#What-is-WOW64" class="headerlink" title="What is WOW64?"></a>What is WOW64?</h3><p> It is a subsystem in 64-bit versions of Windows that allows 32-bit applications to run seamlessly on a 64-bit operating system. This feature is included by default in most 64-bit Windows operating systems.</p><p>At this point, you might have guessed what this challenge is about. The binary given tries to execute arbitrary 64-bit code from the given 32-bit PE file. How does it do that? We will look at this later. For now let’s move on to the other functions.</p><h3 id="Next-up-sub-416E80"><a href="#Next-up-sub-416E80" class="headerlink" title="Next up - sub_416E80"></a>Next up - <code>sub_416E80</code></h3><p><img src="/images/posts/lament_of_the_seraph/6.png" alt=""></p><p>This function simply registers a vectored exception handler named <code>Handler</code>. Basically, it prepares memory and an exception hook for later use.</p><p>Going back to main, we see <code>dword_40E044</code> which is referenced via <code>Handler</code></p><p><img src="/images/posts/lament_of_the_seraph/7.png" alt=""></p><h3 id="Next-up-Handler"><a href="#Next-up-Handler" class="headerlink" title="Next up - Handler"></a>Next up - <code>Handler</code></h3><p>This function is a Vectored Exception Handler (VEH). It intercepts application crashes to hijack the CPU’s execution path. In this big function, only one part is of interest to us.</p><p><img src="/images/posts/lament_of_the_seraph/8.png" alt=""></p><p>This block handles memory access errors. The ransomware intentionally crashes at specific valid pointers to trigger this block. This is a control-flow obfuscation technique to hide the call to the malicious payload (<code>sub_40286D</code>).</p><p>If the Exception Address happens to be <code>dword_40E044</code> then <code>sub_40286D</code> gets called. Here <code>sub_40286D</code> is the actual malicious stuff the binary tries to do.</p><h3 id="Next-up-sub-40286D"><a href="#Next-up-sub-40286D" class="headerlink" title="Next up - sub_40286D"></a>Next up - <code>sub_40286D</code></h3><p>We can see that this function calls two functions of interest - <code>sub_401FD1</code> and <code>sub_401E9D</code>.</p><p><img src="/images/posts/lament_of_the_seraph/9.png" alt=""></p><h3 id="sub-401FD1"><a href="#sub-401FD1" class="headerlink" title="sub_401FD1"></a><code>sub_401FD1</code></h3><p>The function will look something like this after resolving the hashes:</p><p><img src="/images/posts/lament_of_the_seraph/10.png" alt=""></p><p>Here <code>sub_401C19</code> gets the <code>USERPROFILE</code> path via PEB walking, which then builds the path to <code>\\AppData\\Roaming\\Electrum\\recent_servers</code>. The function then parses this file and looks for a string between double quotes whose md5 hash matches <code>5fc44255053d10f73c65104fa689843f</code>.</p><p>Now, it is impossible to get the string from the md5 hash alone, this is where the description comes in which hints at a <code>public server</code>. The intent behind this part is for the player to research on well known public <code>Electrum</code> servers and <code>Electrum</code> as a whole. The required string is <code>electrum.blockstream.info</code> which can be found in <a href="https://github.com/spesmilo/electrum/blob/0eefcbae9cb19ff9ece8cd07dc97f8835b690b83/electrum/chains/mainnet/servers.json">https://github.com/spesmilo/electrum/blob/0eefcbae9cb19ff9ece8cd07dc97f8835b690b83/electrum/chains/mainnet/servers.json</a> or <a href="https://1209k.com/bitcoin-eye/ele.php">https://1209k.com/bitcoin-eye/ele.php</a></p><p>Upon passing the md5 check, the string is returned via <code>Destination</code>.</p><h3 id="Next-up-sub-401E9D"><a href="#Next-up-sub-401E9D" class="headerlink" title="Next up - sub_401E9D"></a>Next up - <code>sub_401E9D</code></h3><p>The string stored in <code>Destination</code> is now passed to this function. We additionally see that <code>VirtualProtect</code> is used in this function, now we can make an educated guess that some shellcode is being executed in this function.</p><p><img src="/images/posts/lament_of_the_seraph/11.png" alt=""></p><p>Our shellcode is stored at <code>unk_40A020</code>.</p><p><img src="/images/posts/lament_of_the_seraph/12.png" alt=""></p><p>Before we analyse our shellcode, lets quickly look at <code>sub_402531</code>.</p><h3 id="sub-402531"><a href="#sub-402531" class="headerlink" title="sub_402531"></a><code>sub_402531</code></h3><p>This function gets files under <code>\\AppData\\Roaming\\Electrum\\wallets</code> and AES-256-CBC encrypts them using <code>openssl</code>. The encrypted file is then saved with the <code>.seraph</code> extension. The IV is hardcoded so  finding the key should be our next logical step. The function takes in the hex of the key and the key is the string returned by <code>sub_401E9D</code> so we can make an educated guess that our shellcode returns the key after some transformations.</p><h2 id="Shellcode-Analysis"><a href="#Shellcode-Analysis" class="headerlink" title="Shellcode Analysis"></a>Shellcode Analysis</h2><p>Lets look at the disassembly of the shellcode stored in <code>unk_40A020</code>,</p><p><img src="/images/posts/lament_of_the_seraph/13.png" alt=""></p><p>Interesting, a <code>retf</code> instruction. This part of the challenge requires further research by the player on the <code>retf</code> or <code>far return</code> instruction and the <code>cs</code> (code segment) register.</p><p>When executing a <code>far return</code> (<code>retf</code>), the processor pops the return instruction pointer (IP) from the top of the stack into the <code>ip</code> register, and then pops the segment selector (CS) from the top of the stack into the <code>cs</code> register, effecting the mode switch.</p><p>From our disassembly, we can see that the <code>cs</code> register holds the value <code>0x33</code> (after the arithmetic operations).</p><h3 id="The-Code-Segment-Register"><a href="#The-Code-Segment-Register" class="headerlink" title="The Code Segment Register"></a>The Code Segment Register</h3><p>In x86 architecture, the CS register holds the segment selector for the code segment. It can have two values - <code>0x23</code> signifying <code>x86</code> instructions, and <code>0x33</code> signifying <code>x64</code> instructions. This challenge utilizes this property of the <code>cs</code> register to execute arbitrary <code>64-bit</code> code in the given <code>32-bit</code> executable. This technique is known as the <strong><code>Heaven&#39;s Gate</code></strong> technique.</p><h2 id="Shellcode-Analysis-Continued"><a href="#Shellcode-Analysis-Continued" class="headerlink" title="Shellcode Analysis (Continued)"></a>Shellcode Analysis (Continued)</h2><p>Now lets look at the hex view of the shellcode:</p><p><img src="/images/posts/lament_of_the_seraph/14.png" alt=""></p><p>Here we see two <code>CB</code> or <code>retf</code> instructions. Lets look at the second <code>far return</code> now,</p><p><img src="/images/posts/lament_of_the_seraph/15.png" alt=""></p><p>Well, now the value of the <code>cs</code> register is <code>0x23</code>, as expected the context now switches from <code>64bit</code> to <code>32bit</code>, which means the code between the two <code>far return</code>s is 64bit instructions embedded in a 32bit executable.</p><p>To summarize, there is 64-bit code getting executed in the given 32-bit executable. This technique, although pretty simple, evades the eyes of multiple antiviruses and is a malicious technique.</p><p>So now how to go ahead? The simplest way is to extract the shellcode, save it in another file and open it in 64-bit mode.</p><h2 id="64bit-Analysis"><a href="#64bit-Analysis" class="headerlink" title="64bit Analysis"></a>64bit Analysis</h2><p>To analyse the shellcode, we need to save it in a different file and open it in 64 bit mode in IDA.</p><p>In the shellcode, we see the function prologue after 32 bytes, and it also so happens that the AES key required is of 32 bytes size. here is the disassembly after the 32 bytes:</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs asm">seg000:0000000000000020                 push    rbx<br>seg000:0000000000000021                 push    rsi<br>seg000:0000000000000022                 push    rdi<br>seg000:0000000000000023                 mov     r8, 0AAAAAAAAAAAAAAAAh<br>seg000:000000000000002D                 mov     rcx, 19h<br>seg000:0000000000000034                 mov     r9, 0BBBBBBBBBBBBBBBBh<br>seg000:000000000000003E                 mov     rdi, 20h ; &#x27; &#x27;<br>seg000:0000000000000045                 mov     r10, 0CCCCCCCCCCCCCCCCh<br>seg000:000000000000004F                 xor     rbx, rbx<br>seg000:0000000000000052<br>seg000:0000000000000052 loc_52:                                 ; CODE XREF: seg000:0000000000000075↓j<br>seg000:0000000000000052                 cmp     rbx, rdi<br>seg000:0000000000000055                 jnb     short loc_77<br>seg000:0000000000000057                 mov     al, [r9+rbx]<br>seg000:000000000000005B                 mov     rax, rbx<br>seg000:000000000000005E                 xor     rdx, rdx<br>seg000:0000000000000061                 div     rcx<br>seg000:0000000000000064                 mov     dl, [r8+rdx]<br>seg000:0000000000000068                 mov     al, [r9+rbx]<br>seg000:000000000000006C                 xor     al, dl<br>seg000:000000000000006E                 mov     [r10+rbx], al<br>seg000:0000000000000072                 inc     rbx<br>seg000:0000000000000075                 jmp     short loc_52<br>seg000:0000000000000077 ; ---------------------------------------------------------------------------<br>seg000:0000000000000077<br>seg000:0000000000000077 loc_77:                                 ; CODE XREF: seg000:0000000000000055↑j<br>seg000:0000000000000077                 pop     rdi<br>seg000:0000000000000078                 pop     rsi<br>seg000:0000000000000079                 pop     rbx<br>seg000:000000000000007A                 push    2<br>seg000:000000000000007C                 pop     rax<br>seg000:000000000000007D                 shl     eax, 4<br>seg000:0000000000000080                 add     al, 3<br>seg000:0000000000000082                 push    rax<br>seg000:0000000000000083                 push    0<br>seg000:0000000000000088                 retfq<br></code></pre></td></tr></table></figure><p>Following is the annotated shellcode for better clarity:<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs asm">mov r8, 0xAAAAAAAAAAAAAAAAh   ; xor_key pointer<br>mov rcx, 19h                  ; key_len<br>mov r9, 0xBBBBBBBBBBBBBBBBh   ; encrypted_data pointer<br>mov rdi, 20h                  ; encrypted_data size (32 bytes)<br>mov r10, 0xCCCCCCCCCCCCCCCCh  ; output buffer pointer<br>xor rbx, rbx                  ; counter = 0<br></code></pre></td></tr></table></figure><br>Addresses like <code>0xAAAAAAAAAAAAAAAA</code> get patched at runtime, hence placeholder text in its hex view.</p><p>After this there is the main transformation of the encrypted data:<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs asm">xor_loop:<br>    cmp rbx, rdi              ; if (counter &gt;= 32)<br>    jnb done<br><br>    mov al, [r9+rbx]          ; encrypted_byte = encrypted_data[counter]<br>    mov rax, rbx<br>    xor rdx, rdx<br>    div rcx                   ; rax = counter / key_len, rdx = counter % key_len<br><br>    mov dl, [r8+rdx]          ; key_byte = xor_key[counter % key_len]<br>    mov al, [r9+rbx]<br>    xor al, dl                ; decrypted = encrypted ^ key_byte<br>    mov [r10+rbx], al         ; output[counter] = decrypted<br><br>    inc rbx                   ; counter++<br>    jmp xor_loop<br><br></code></pre></td></tr></table></figure></p><p>after which the registers get popped and the cs register gets modified to return back to 32 bit mode:</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs asm">done:<br>    pop rdi                   ; Restore saved registers<br>    pop rsi<br>    pop rbx<br><br>    push 2<br>    pop rax<br>    shl eax, 4<br>    add al, 3<br>    push rax<br>    push 0<br>    retfq                     ; far return to switch back to 32 bit mode<br></code></pre></td></tr></table></figure><p>Right, so the algorithm is our classic repeating-key XOR cipher. Below is a comparison between the disassemblies in 32-bit and 64-bit mode respectively:</p><p><img src="/images/posts/lament_of_the_seraph/16.png" alt=""></p><p>Now, we know the xor key and the encrypted bytes, that’s the only two things required to get the flag:</p><figure class="highlight python"><table><tr><td class="code"><pre><code class="hljs python">data = <span class="hljs-built_in">bytes</span>.fromhex(<span class="hljs-string">&quot;0B 05 11 06 0F 31 27 34 7E 36 5C 2D 31 5B 20 1A 46 2B 26 5E 62 3A 5A 12 3B 2D 5F 02 57 00 41 08&quot;</span>)<br>key = <span class="hljs-string">&quot;electrum.blockstream.info&quot;</span><br>result = <span class="hljs-built_in">bytes</span>([data[i] ^ <span class="hljs-built_in">ord</span>(key[i % <span class="hljs-built_in">len</span>(key)]) <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-built_in">len</span>(data))])<br><span class="hljs-built_in">print</span>(result.decode())<br><span class="hljs-comment"># nite&#123;CRYPT0BR0Sn4NG3LS4tTH3g4t3&#125;</span><br></code></pre></td></tr></table></figure>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>niteCTF 2025 - Ophelia&#39;s Truth</title>
      <link href="/2025/12/15/ophelias_truth/"/>
      <url>/2025/12/15/ophelias_truth/</url>
      
        <content type="html"><![CDATA[<h1 id="Ophelia’s-Truth-1"><a href="#Ophelia’s-Truth-1" class="headerlink" title="Ophelia’s Truth 1"></a>Ophelia’s Truth 1</h1><blockquote><p>A detective at Moscow PD, Department 19, receives a message asking him to check the forensic analysis portal for a DNA report. Attached to the message is a file containing a link to the portal. He opens the attachment, but initially, nothing seems to happen, so he overlooks it. Later, he realizes that a crucial file from an ongoing case has gone missing.</p><p>He has provided the forensic artifacts from his computer to you, his colleague at the cyber forensics department, to figure out what went wrong. Find:</p><ul><li>The filename of the attachment</li><li>The ip from where the malware was executed</li><li>The CVE the attacker exploited</li></ul><p>Flag format: nite{file_name.ext_XXX.XXX.XXX.XXX_CVE-XXXX-XXXXX}</p><p>Challenge Link: <a href="https://drive.proton.me/urls/WTX8DDNBWG#gtNS5LAjkzfN">https://drive.proton.me/urls/WTX8DDNBWG#gtNS5LAjkzfN</a></p></blockquote><p><strong>Flag:</strong> <code>nite{dna_analysis_portal.url_10.72.5.205_CVE-2025-33053}</code></p><p>Keeping the description in mind, we start by searching for relevant file artifacts using <code>filescan</code>. The extension <code>.url</code> is a common shortcut format in Windows, often used for web links.</p><p>We can search for files containing “dna” or with the <code>.url</code> extension:<br><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">python vol.py -f ophelia.raw windows.filescan.FileScan | grep dna<br><br>0xc50d0c562a90.0\Users\Igor\Documents\Important Links\dna_analysis_portal.url<br></code></pre></td></tr></table></figure></p><p>Next, we extract the file content to understand its behavior. We use the virtual address found in the previous step:<br><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">python vol.py -f ophelia.raw windows.dumpfiles.DumpFiles --virtaddr=0xc50d0c562a90<br></code></pre></td></tr></table></figure><br>Despite the “Error dumping file” message the content is successfully dumped. The content of the dumped <code>.url</code> file reveals a specific structure:<br><figure class="highlight ini"><table><tr><td class="code"><pre><code class="hljs ini"><span class="hljs-section">[InternetShortcut]</span><br><span class="hljs-attr">URL</span>=C:\Program Files\Internet Explorer\iediagcmd.exe<br><span class="hljs-attr">WorkingDirectory</span>=\\<span class="hljs-number">10.72</span>.<span class="hljs-number">5.205</span>\webdav\\<br><span class="hljs-attr">ShowCommand</span>=<span class="hljs-number">7</span><br><span class="hljs-attr">IconIndex</span>=<span class="hljs-number">13</span><br><span class="hljs-attr">IconFile</span>=C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe<br><span class="hljs-attr">Modified</span>=<span class="hljs-number">20</span>F06BA06D07BD014D<br></code></pre></td></tr></table></figure></p><p>This is not a standard web shortcut. Instead of pointing to a website, the <code>URL</code> field points to a local system binary: <code>iediagcmd.exe</code> and the <code>WorkingDirectory</code> is set to a remote UNC path: <code>\\10.72.5.205\webdav\\</code>.</p><p>This configuration matches the signature of <strong>CVE-2025-33053</strong>.</p><p>When the user clicks this link, Windows executes <code>iediagcmd.exe</code>. This legitimate helper program attempts to launch another executable (in our case it happens to be <code>route.exe</code>). Because the <code>WorkingDirectory</code> is hijacked to point to the attacker’s WebDAV server, <code>iediagcmd.exe</code> inadvertently loads and executes <code>route.exe</code> hosted on <code>10.72.5.205</code> instead of the expected local file.</p><h1 id="Ophelia’s-Truth-2"><a href="#Ophelia’s-Truth-2" class="headerlink" title="Ophelia’s Truth 2"></a>Ophelia’s Truth 2</h1><blockquote><p>Our initial analysis suggests that the attacker executed a dropper on the victim’s machine via RCE, but the executable shows signs of partial corruption, affecting its tailing ends. The malware appears to have deployed an additional executable on the system. Find out:</p><ul><li>The name of the dropped executable.</li><li>The paths where the encrypted original malware (dropper) is stored currently.</li><li>The key used to decrypt and execute the original malware.</li></ul><p>Flag format: nite{dropped.exe_SOME\EXAMPLE\Path\1_SOME\EXAMPLE\Path\2_SOME\EXAMPLE\Path\3_keyinhex}</p></blockquote><p><strong>Flag:</strong> <code>nite{RuntimeBroker.exe_HKEY_CURRENT_USER\SOFTWARE\Skype\Username_HKEY_CURRENT_USER\SOFTWARE\Skype\LastProfile_HKEY_CURRENT_USER\SOFTWARE\Skype\SkypePath_90b2cca7c0631aeda7ab1221c98fb1803a6dce2808efdd99e94f5e7cad3a4e78}</code></p><p>Now as the description mentions that the dropper dropped a executable onto the system, we can start looking for executables on the victims system and look for inconsistencies.</p><p>After some searching you will find that a <code>RuntimeBroker.exe</code> is located at <code>\Users\Igor\AppData\Local\Temp\RuntimeBroker.exe</code>. RuntimeBroker is a system executable and is only present at <code>\Windows\System32\</code>. Now we dump the executable for further analysis.</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">python vol.py -f ophelia.raw windows.dumpfiles.DumpFiles --virtaddr=0xc201a7024e20<br>Volatility 3 Framework 2.26.2<br>Progress:  100.00               PDB scanning finished<br>Cache   FileObject      FileName        Result<br><br>ImageSectionObject      0xc201a7024e20  RuntimeBroker.exe       file.0xc201a7024e20.0xc2019fddaa20.ImageSectionObject.RuntimeBroker.exe.img<br></code></pre></td></tr></table></figure><p>Decompiling and analysing the dumped exe will tell us the following:</p><ul><li><p>All of the paths and strings are obfuscated with the XOR key <code>0xCAFEBABE</code>.</p></li><li><p>It retrieves strings from three specific values: <code>Username</code>, <code>LastProfile</code>, and <code>SkypePath</code>. These strings are are then concatenated together.</p></li><li><p>Then searches for files in the user’s <code>%TEMP%</code> directory with the pattern <code>wct*.tmp</code>. It then checks if the size of the file equals <code>64</code> bytes. This is a strong indicator that the file contains a hex-encoded 256-bit key.</p></li><li><p>Once the key is read from the temp file, the malware AES-256-CBC decrypts the concated registry string. The IV is not read from a file but is hardcoded. It is stored as an obfuscated byte array. The IV happens to be <code>9b5c9b7e83cdfbdf11f87195c3192a47</code></p></li><li><p>After successful decryption, the malware generates a pseudo-random filename beginning with <code>WUDFHost_</code> (mimicking the Windows User-mode Driver Framework Host) and ending with <code>.exe</code>. It writes the decrypted bytes to this file in the <code>%TEMP%</code> directory and immediately executes it.</p></li></ul><p>This represents a multi-stage infection where the first stage encrypts and hides itself, while this second stage (RuntimeBroker.exe) acts as the loader that reconstructs and executes the original malware.</p><p>The original malware paths reconstruct to - <code>HKEY_CURRENT_USER\SOFTWARE\Skype\Username</code>, <code>HKEY_CURRENT_USER\SOFTWARE\Skype\LastProfile</code> and <code>HKEY_CURRENT_USER\SOFTWARE\Skype\SkypePath</code></p><p>Now to get the key, you can <code>filescan</code> for files that have <code>wct</code> in their name:</p><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">python vol.py -f ophelia.raw windows.filescan.FileScan | grep wct<br><br>0xc201a5c8ba40.0\Users\Igor\AppData\Local\Temp\wct8774.tmp<br>0xc201a702fd20  \Users\Igor\AppData\Local\Temp\wct3550.tmp<br></code></pre></td></tr></table></figure><p>But dumping them wont work. We need to find an alternative, we know for a fact that the original dropper saved the key in one of these files which the dropped exe uses, so its definite that the key is somewhere in the memory its just that volatility has failed to map the contents of the file to the key files address. So instead one needs to look for strings that are 64 bytes in length via strings.</p><p>The dump contains a large number of 64‑byte strings, so identifying the correct key requires generating a wordlist of all potential candidates. Each key should be tested with the known IV to decrypt the Skype registry data. If we look at the description, it mentions that the executable is corrupted at its later sections. But since AES is a block cipher, we can still recover the correct key by decrypting just the initial blocks and checking for the ‘MZ’ signature.</p><p>The key which decrypts all the registries successfuly to give an executable is the correct key.</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">python vol.py -f ophelia.raw windows.registry.printkey --key &quot;Software\\Skype&quot; &gt; skype.txt<br></code></pre></td></tr></table></figure><p>We can write a <a href="decrypt_skype.py">python script</a> to decrypt the registries (we can just focus on the first registry <code>Username</code> and look for the MZ bytes).<br><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ciphertext: 1668096 bytes, keys: 1253<br>FOUND AES‑256 KEY: 90b2cca7c0631aeda7ab1221c98fb1803a6dce2808efdd99e94f5e7cad3a4e78<br></code></pre></td></tr></table></figure></p><p>The list of potential keys is here: <a href="potentialkeys.txt">potentialkeys.txt</a></p><p>Hence, the final flag is: <code>nite{RuntimeBroker.exe_HKEY_CURRENT_USER\SOFTWARE\Skype\Username_HKEY_CURRENT_USER\SOFTWARE\Skype\LastProfile_HKEY_CURRENT_USER\SOFTWARE\Skype\SkypePath_90b2cca7c0631aeda7ab1221c98fb1803a6dce2808efdd99e94f5e7cad3a4e78}</code></p><p>Another valid flag is: <code>nite{RuntimeBroker.exe_HKCU\SOFTWARE\Skype\Username_HKCU\SOFTWARE\Skype\LastProfile_HKCU\SOFTWARE\Skype\SkypePath_90b2cca7c0631aeda7ab1221c98fb1803a6dce2808efdd99e94f5e7cad3a4e78}</code></p><p>Challenge Source Code:</p><ul><li><a href="RuntimeBroker.cpp">RuntimeBroker.exe Source</a></li></ul><h1 id="Ophelia’s-Truth-3"><a href="#Ophelia’s-Truth-3" class="headerlink" title="Ophelia’s Truth 3"></a>Ophelia’s Truth 3</h1><blockquote><p>Igor discovers that the missing file was actually CCTV footage related to the case surrounding his mother’s death. With this revelation, the investigation now shifts toward the dropper. Your task is to determine what the dropper does and recover the missing file. After extensive efforts by the cyber forensics department, they have successfully recovered an uncorrupted version of the dropper, and it has now been provided to you for analysis. Find:</p><ul><li>The name of the file that went missing</li><li>Name of the file where its now hidden</li><li>Size of the original file in bytes</li><li>Number of frames in the original file</li><li>Dimensions of the original file</li></ul><p>Along with the challenge file from Ophelia’s Truth 1, the attached file is also required to solve the challenge.</p><p><code>Note: The binary is a live malware, please exercise caution.</code></p><p>File Link: <a href="https://drive.proton.me/urls/J88R557TTW#ZYm4lTfXFEOS">https://drive.proton.me/urls/J88R557TTW#ZYm4lTfXFEOS</a></p><p>Password: infected</p><p>Flag format: nite{cctv_footage_file.ext_hidden_file.ext_12345_12345_12345X12345}</p></blockquote><p><strong>Flag:</strong> <code>nite{20250627_103005_CAM01.avi_thumbcache_777.db_483596_128_1280X720}</code></p><p>Upon decompiling the given executable, we can see that it does the following:</p><ul><li>The malware iterates through the user’s <code>%USERPROFILE%\Videos</code> directory. It specifically searches for a file named <code>20250627_103005_CAM01.avi</code>.</li><li><p>Once found, the file moved and encrypted. The malware reads the video file, encrypts it, and saves it to: <code>%LOCALAPPDATA%\Microsoft\Windows\Explorer\thumbcache_777.db</code>. The original video file is then deleted.</p></li><li><p>To ensure continued access, the malware establishes persistence by hijacking a legitimate application. It modifies the <strong>Image File Execution Options (IFEO)</strong> registry key for <code>AcroRd32.exe</code> (Adobe Acrobat Reader) and sets dropped payload as “Debugger” So every time the user opens a PDF, Windows checks this registry key and launches the “Debugger” (our malware).</p></li><li><p>The malware statically links the <strong>Crypto++</strong> library. Then the <code>.avi</code> file is encrypted using DES in ECB mode. The key is hardcoded in the binary. The key happens to be <code>6B 73 79 70 75 6E 6E 0B</code>.</p></li></ul><p>To get the key, one can take advantage of a peculiar thing the binary does. You will see that <code>sub_14008BDF0</code> is where all the paths are initialized, with the <code>wct*.tmp</code> path (AES key path) also being initialized in the same function. This function gets called in <code>sub_14008BCC0</code> and it can be seen that the same path is used to store the AES key as well as the DES key.</p><p>First the DES key gets stored via <code>sub_14008B4B0</code> and then later the AES key gets stored in the same path via <code>sub_14008A6F0</code>. So if we set up a breakpoint after <code>sub_14008B4B0</code> and run it in a VM, we can get the key.</p><p><img src="/images/posts/ophelias_truth/1.png" alt=""></p><p>Now you can run it and look for files of size 8 bytes (as we know thats the size of the DES key)<br><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">find <span class="hljs-string">&#x27;./AppData/Local/Temp&#x27;</span> -maxdepth 1 -name <span class="hljs-string">&quot;wct*.tmp&quot;</span> -size 8c<br>./AppData/Local/Temp/wctCCD7.tmp<br><br>xxd <span class="hljs-string">&#x27;./AppData/Local/Temp/wctCCD7.tmp&#x27;</span><br>00000000: 6b73 7970 756e 6e0b                      ksypunn.<br></code></pre></td></tr></table></figure></p><p>Now we know that the encrypted file is stored as <code>thumbcache_777.db</code>, lets try filescan on it:<br><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"> python vol.py -f ophelia.raw windows.filescan.FileScan | grep thumbcache<br><br>0xc201a3dc8910.0\Users\Igor\AppData\Local\Microsoft\Windows\Explorer\thumbcache_777.db<br>0xc201a485c900  \Windows\System32\thumbcache.dll<br>0xc201a485dd50  \Users\Igor\AppData\Local\Microsoft\Windows\Explorer\thumbcache_16.db<br>0xc201a485ecf0  \Users\Igor\AppData\Local\Microsoft\Windows\Explorer\thumbcache_idx.db<br>0xc201a4868480  \Users\Igor\AppData\Local\Microsoft\Windows\Explorer\thumbcache_48.db<br>0xc201a486c490  \Users\Igor\AppData\Local\Microsoft\Windows\Explorer\thumbcache_256.db<br>0xc201a4b7ec60  \Users\Igor\AppData\Local\Microsoft\Windows\Explorer\thumbcache_32.db<br></code></pre></td></tr></table></figure></p><p>Now we can try dumping, but again just like the key file in the last challenge, it won’t work so we need to look for an alternative. The trick here is to look for common bytes in <code>avi</code> files, decrypt accordingly, then search for the encrypted bytes in the dump.</p><p>There are a bunch of common bytes, for eg - <code>RIFF</code>, <code>AVI</code> (the file signatures) but DES requires 8 byte blocks so we need longer sequence of bytes such as <code>hdrlavih8</code>, <code>strlstrh8</code>, etc. The longer sequence of bytes is not universal but very common and should be worth a try.</p><p>But the logical thing to try is encrypt <code>AVI LIST</code> as LIST is what is used to list the chunks of the AVI file and is usually what follows after the AVI header.</p><p>The encrypted hex of <code>AVI LIST</code> is <code>85 88 5b ac 9f f4 75 5d</code>.</p><p>Now lets search for these hex in the dump.<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">xxd ophelia.raw | grep -E &#x27;8588 5bac 9ff4 755d&#x27; -A 10 -B 1<br><br>1b78ca850: 0000 0000 0000 0000 0000 0000 46b5 b743  ............F..C<br>1b78ca860: c23d 31ea 8588 5bac 9ff4 755d 85c8 477c  .=1...[...u]..G|<br>1b78ca870: 48fc 856f 905f d1a5 576b 6ad2 d243 9825  H..o._..Wkj..C.%<br>1b78ca880: e5a9 de74 d3bb 0dcd 3bb1 a3bb 5b05 4acd  ...t....;...[.J.<br>1b78ca890: 5875 b3cc c036 bc41 ec10 9eef f838 748d  Xu...6.A.....8t.<br>1b78ca8a0: 2ab0 4116 1d61 0b3a fcd5 730a 1d61 0b3a  *.A..a.:..s..a.:<br>1b78ca8b0: fcd5 730a 3ee3 1249 6184 0ce5 f6d4 689b  ..s.&gt;..Ia.....h.<br>1b78ca8c0: 7e50 38cb b7d9 651d febb fd43 47d7 392a  ~P8...e....CG.9*<br>1b78ca8d0: f53f d7ea 1d61 0b3a fcd5 730a 74d7 3f5e  .?...a.:..s.t.?^<br>1b78ca8e0: 5ec4 331d 2757 08a7 33a6 26f0 90b0 b9fb  ^.3.&#x27;W..3.&amp;.....<br>1b78ca8f0: f010 a012 1d61 0b3a fcd5 730a 65dd 0768  .....a.:..s.e..h<br>1b78ca900: 3ced 1f7a cb8f 16b2 8587 0408 f838 748d  &lt;..z.........8t.<br></code></pre></td></tr></table></figure></p><p>Voila!<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1b78ca860: 46b5 b743 c23d 31ea 8588 5bac 9ff4 755d<br>                               └──────────────────┘<br>                               Encrypted &quot;AVI LIST&quot;<br></code></pre></td></tr></table></figure></p><p>Now looking at the 8 bytes before the encrypted AVI LIST we get - <code>46b5 b743 c23d 31ea</code> which is just <code>RIFF &lt;file size&gt;</code>. We can decrypt these 8 bytes to get the size of the avi file.</p><p>The bytes <code>46 b5 b7 43 c2 3d 31 ea</code> upon decryption is  <code>82 73 70 70 4 97 7 0</code>. Which corresponds to <code>483,588 bytes</code>. Now this is actually the size of the file post the 8 bye header, so to get the total size we add <code>8</code> to <code>483,588</code>, thus the total size of the file is <code>483,596</code> bytes.</p><p>To get the information required for the flag we need to focus on the starting few bytes of the file, then decrypt accordingly. The decrypted file looks something like this:<br><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">00000000: 5249 4646 0461 0700 4156 4920 4c49 5354  RIFF.a..AVI LIST<br>00000010: 3812 0000 6864 726c 6176 6968 3800 0000  8...hdrlavih8...<br>00000020: 3582 0000 a861 0000 0000 0000 1009 0000  5....a..........<br>00000030: 8000 0000 0000 0000 0100 0000 0000 1000  ................<br>00000040: 0005 0000 d002 0000 0000 0000 0000 0000  ................<br>00000050: 0000 0000 0000 0000 4c49 5354 e010 0000  ........LIST....<br>00000060: 7374 726c 7374 7268 3800 0000 7669 6473  strlstrh8...vids<br>00000070: 464d 5034 0000 0000 0000 0000 0000 0000  FMP4............<br>00000080: 0100 0000 1e00 0000 0000 0000 8000 0000  ................<br>00000090: ddb8 0000 ffff ffff 0000 0000 0000 0000  ................<br>000000a0: 0005 d002 7374 7266 2800 0000 2800 0000  ....strf(...(...<br>000000b0: 0005 0000 d002 0000 0100 1800 464d 5034  ............FMP4<br>000000c0: 0030 2a00 0000 0000 0000 0000 0000 0000  .0*.............<br>000000d0: 0000 0000 4a55 4e4b 1810 0000 0400 0000  ....JUNK........<br>000000e0: 0000 0000 3030 6463 0000 0000 0000 0000  ....00dc........<br><br></code></pre></td></tr></table></figure><br>To get number of frames we look at bytes at offset <code>0x30</code>, we get number of frames as <code>128</code>. And at offset <code>0xd0</code> we get <code>0005 0000 d002 0000</code> which translates to <code>1280</code> pixels in height and <code>720</code> pixels in width.</p><p>Hence, our final flag - <code>nite{20250627_103005_CAM01.avi_thumbcache_777.db_483596_128_1280X720}</code></p><p>Challenge Source Code:</p><ul><li><a href="route.cpp">route.exe Source</a></li></ul>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
